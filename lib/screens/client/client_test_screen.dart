import 'package:flutter/material.dart';

class ClientTestScreen extends StatelessWidget {
  const ClientTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Hiii! You logged in!"),
    );
  }
}
