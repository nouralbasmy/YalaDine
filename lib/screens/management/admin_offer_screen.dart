import 'package:flutter/material.dart';

class AdminOfferScreen extends StatelessWidget {
  const AdminOfferScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Offers Screen');
  }
}
